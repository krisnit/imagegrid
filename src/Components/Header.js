import React from "react";

const Header = () => {
  return (
    <div>
      <form>
        <input type="text"></input>
        <button>Search</button>
      </form>
    </div>
  );
};

export default Header;
